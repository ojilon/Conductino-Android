// Root build file — plugin versions only, no project-wide deps.
plugins {
    id 'com.android.application' version '8.13.2' apply false
}

//tasks.register('clean', Delete) {
  //  delete rootProject.layout.buildDirectory
//}