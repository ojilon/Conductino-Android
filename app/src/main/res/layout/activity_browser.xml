<?xml version="1.0" encoding="utf-8"?>
<!--
  The Activity layout is deliberately minimal: a single full-bleed WebView.
  All chrome (URL bar, tabs, buttons) is drawn by the HTML/CSS/JS UI states
  loaded into this WebView, so Java can swap the entire experience per state.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/aurora_bg">

    <WebView
        android:id="@+id/web_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
